const inquirer = require('inquirer');

function promptUser() {
  console.log('Hi, manager view here');
  // inquirer.prompt([{}
  // ])
  // .then(answers => {

  // })
  // .catch(error => {
  //   throw error;
  // })
}

module.exports = {
  promptUser: promptUser
}